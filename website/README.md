# BattleLuck NPC Quests

An NPC contract and world-planning interface for BattleLuck. The default workspace is
the quest board; the existing interactive Vardoran map remains available from the
workspace switcher.

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
